import getSecurityKeysStatus from '../getSecurityKeysStatus';

describe('getSecurityKeysStatus helper', () => {
  it.todo('returns succeeded and failed hints correctly');
  it.todo('returns the expected result if conditions are RegExp');
  it.todo('returns the expected result if conditions are boolean methods');
});
