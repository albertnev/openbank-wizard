import getRenderedText from '../getRenderedText';

describe('getRenderedText helper', () => {
  it.todo('returns the text correctly if it is sent as a string');
  it.todo(
    'returns the rendered result of the method provided if it is sent as a method'
  );
});
